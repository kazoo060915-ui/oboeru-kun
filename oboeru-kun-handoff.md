# 覚える君 移植 引き継ぎ書

対象: Antigravity（またはClaude Code拡張機能）で作業するAIエージェント宛
作成日: 2026-08-12
現状: Claudeアーティファクト上でReactプロトタイプが動作中（`oboeru-kun.jsx` 同梱）

---

## 0. これは何か

AI Driven School（ADS）の卒業制作の一部。日常のちょっとした面倒を解決するツール群のうち、
「専門用語を覚えても時間が経つと忘れる」問題を解決する、パーソナル復習コーチ。

**一言で言うと**: 「AIに聞けば分かる。でも、忘れたことは聞けない。」
AIへの質問は"入口"（分からないと気づいた時にしか発動しない）。
このツールは"出口"（忘れたタイミングで向こうから聞いてくる）。競合ではなく、空いていた穴を埋めるもの。

---

## 1. 絶対に壊してはいけない設計思想

移植作業でコードは全部書き直して構わないが、以下の**設計判断はそのまま踏襲**すること。
UIの見た目より、この思想の再現度の方が重要。

1. **出力を先に強制する。** 用語の説明を見せる前に、必ず自分の言葉で説明させる。
   「読んで分かった気になる（流暢性の錯覚）」を壊すのがこのツールの核。
2. **ツッコミは人格攻撃にならない。** 間違いを笑いに変えるが、馬鹿にしない。
   「わからん」という回答も責めずに拾う。ここが崩れると毎日開くのが苦痛になり離脱する。
3. **聞き返しチャットは「答えた後」にしか出さない。** お題が出ている最中に解答を見に行けると、
   自力で思い出そうとする時間（＝定着が起きる瞬間）が消える。UI配置として厳守。
4. **正解を一つ返すだけで終わらせない。** 簡単な言い方・比喩・例え・「今すぐ手を動かすミッション」
   など、複数の角度から説明できること。角度を変える機能そのものが差別化ポイント。
5. **忘却曲線に沿って向こうから聞いてくる。** ユーザーが思い出す前に、システム側が
   タイミングを管理する。ここが「AIに聞く」との根本的な違い。

---

## 2. 現状のプロトタイプ（参照実装）

同梱の `oboeru-kun.jsx` が動く仕様書。移植先で以下をそのまま（あるいは改善して）再現する。

- 用語カードの一覧・追加（用語＋「どこで出てきたか」のメモ）
- 復習キュー（`next <= today` の用語を出題）
- 自己説明 → Claude API採点 → JSON構造化レスポンス
  （`score` / `tsukkomi` / `correct` / `missed[]` / `mission`）
- スコアに応じた間隔リピティション（レベル0〜4、間隔 1/3/7/14/30日、
  80点以上で昇格、50点未満でレベル0に戻す）
- 採点後の聞き返しチャット（文脈付き、専門用語は使わず関西弁で3文以内、
  ユーザー自身の固有名詞には断定しない）
- 音声入力（Web Speech API、`ja-JP`、説明欄と聞き返しチャットの両方）
- 見た目のトーン: 判子・スタンプ演出、暖色（黄土色×朱色×墨色）、太い罫線、印刷物っぽい質感

プロンプト文言（採点用・チャット用）は現状かなり調整済みなので、大きく変えずに移植し、
移植後に実運用しながら微調整する方針でよい。

---

## 3. 今回の移植で解決したい、ブラウザ内アーティファクトの限界

| 制約 | 内容 |
|---|---|
| 通知が出せない | 復習コーチの心臓部。今は本人が自主的に開かないと成立していない |
| データがブラウザ内に閉じている | Claudeのartifact storageのみ。他端末・他ブラウザから見えない |
| ローカルファイルに触れない | Cursorのログ／ADS講義メモ／VTT修正履歴などの自動取り込みが不可能 |
| API課金がAnthropic持ち | 自前アプリにすると自分のAPIキーで払う（Sonnetで採点1回数円程度の想定） |

---

## 4. 移植要件

### 4.1 技術スタック
- Next.js（App Router）+ Vercelデプロイ
- DB: Supabase推奨（認証・DB・cronがセットで揃うため。他案がベターならそちらでも可）
- 採点・チャット: Anthropic API（Claude Sonnet系）を自分のAPIキーで直接呼ぶ
- 既存資産の再利用: GAS → Claude API → Twitter API パイプライン（毎朝7時トリガー）と
  同じ設計パターンを、通知パイプラインに転用できる

### 4.2 データモデル（叩き台。カラム名などは自由に調整してよい）

```
terms
  id
  user_id
  term            -- 用語
  note            -- どこで出てきたか（自分の実装との紐付け）
  level           -- 0-4
  next_review_at  -- date
  last_score      -- nullable int
  created_at

reviews（履歴。後で「よく間違える用語」の分析に使える）
  id
  term_id
  score
  answer_text
  tsukkomi
  missed_keywords -- jsonb array
  created_at
```

### 4.3 通知（最優先の新規機能）
- 毎朝、その日 `next_review_at <= today` の用語件数をLINE or メールで送る
- 配線は既存のGASパイプラインを流用してよい（朝7時のX投稿と同じ発想の「逆再生」）
- 通知本文はカズ先生の口調に合わせ、開くハードルを下げる短文にする
  （例: 「今日は3件。〇〇と△△、覚えてる？」）

### 4.4 スマホからのアクセス
- 隙間時間（通勤中など）に1問だけ回せることが要件。レスポンシブ必須。
- 音声入力はモバイルブラウザでの動作確認を優先（Web Speech APIの対応状況に注意）

### 4.5 今回は実装しない／保留にする機能（合意済み）
- **作業中のリアルタイム割り込み通知は却下。** フロー状態を壊す。
- 代わりに **「1日の作業ログを振り返る日次ダイジェスト」** は将来やる価値ありと合意。
  - その日のCursorログ／gitの差分／VTT修正履歴などをローカルで拾い、
    「今日ADSのどの概念に触れたか」をClaude APIに判定させる
  - 既存用語に触れていたら復習日を前倒し、新しい用語は自動登録
  - ただしこれは**ローカルにログ監視の小さな常駐スクリプトかエディタ拡張が要る**ため、
    Next.js移植が完了した後の第2フェーズとする。今回のスコープには含めない。

---

## 5. 移植の進め方（推奨）

1. `oboeru-kun.jsx` をUI/UXとプロンプトの仕様書として読み込む
2. Next.jsプロジェクトの雛形を作り、コンポーネントを移植（見た目のトーンは維持）
3. Supabaseでスキーマ作成、storage呼び出しをDBアクセスに置き換え
4. Anthropic APIキーを環境変数化し、サーバー側（Route Handler等）から呼ぶ形に変更
   （クライアントから直接APIキーを叩かない）
5. 通知パイプライン（GAS or Vercel Cron）を接続
6. Vercelにデプロイ、スマホ実機で音声入力とレスポンシブを確認

---

## 6. 判断が必要な未決事項（着手前にユーザーに確認すること）

- 通知はLINEとメール、どちらを優先するか（既存のLステップ活用も選択肢）
- SupabaseのAuthをどう使うか（本人しか使わない前提なら簡易認証でも可）
- 日次ダイジェスト機能は本当に第2フェーズ行きでよいか、それとも骨組みだけ今回作るか

---

## 7. 同梱ファイル

- `oboeru-kun.jsx` — 現行プロトタイプ全文。UIコンポーネント構造、採点プロンプト、
  チャットプロンプト、間隔リピティションのロジックがすべてここに入っている。

---

---

## 8. 最新の進捗状況（2026-08-16 現状）

Next.jsへの完全移植、UI/UX強化、セキュリティ是正、Vercel本番稼働まで全て完了しています。

### ✅ 実装・稼働済みの全機能一覧

1. **本番デプロイ＆稼働**:
   - Vercel本番環境（`https://oboeru-kun.vercel.app`）にて稼働中。
   - Next.js 16 (App Router) + Turbopack + TailwindCSS（和モダンデザイン）。

2. **マルチコーチ＆パーソナライズ（ニックネーム機能）**:
   - 5人の学習コーチ（👦大阪の兄ちゃん、🌸褒め上手な先輩、👔スマートメンター、🔥熱血コーチ、🧙知識の賢者）をいつでも切り替え可能。
   - **学習者のニックネーム登録機能**（「カズ」「田中」など）。AIが「生徒さん」などの事務的呼称を完全排除し、名前で親身に語りかける。

3. **極上のAI解説・採点・チャット（Claude API連携）**:
   - **技術的正体・分類の明記**: 「結局IT的には何なのか（ライブラリ/プロトコル/概念等）」を冒頭で明言。
   - **日本語リズムの超強力な語呂合わせ**: アルファベット記号パズルを廃止し、「最初に・すべて・ガツンと＝SSG」のように音と動作で覚えるフレーズを提案。
   - **有名サービスでの実例**: Amazon、X/Twitter、メルカリ、食べログなど具体的にどの画面で使われているかを最初から解説。
   - **スマホ完全対応の比較表レンダラー**: Markdownテーブルを自動検知し、スマホで崩れず横スクロールできる美しい和風表（黒ヘッダー）で描画。
   - **末尾の質問誘導（呼び水）**: 生徒が「へ〜」で終わらないよう、具体的な疑問を添えてチャット質問を促す。
   - **ワンタップ質問チップ**: 「他と何が違う？」「実務での使い道」「コードのどこに書く？」などを1タップで送信可能。

4. **講義スライド・教材からの厳選用語抽出（FileImporter）**:
   - PDF / 画像 / テキストから、名前のついた概念・技術だけを厳選抽出（個数固定なし。詳細は §10）。
   - 講義の言い回し・行動フレーズ・一般的な日本語のノイズを徹底排除し、複数ファイル跨ぎの重複マージ・既存用語チェックを自動実行。

5. **忘却曲線アルゴリズム＆演出**:
   - レベル0〜4の間隔リピティション（1日/3日/7日/14日/30日）。
   - スコアに応じた和風アニメーション（80点以上の桜吹雪🌸、低スコア時の雨とシェイク🌧️、完了時の紙吹雪🎉）。
   - 音声入力（Web Speech API、スマホ対応）。

6. **セキュリティ＆インフラ**:
   - Supabase RLS（全テーブル完全有効化、Service Role Key経由）。
   - パスコード認証ゲート（Cookieセッション暗号化）。
   - JST（日本標準時）完全統一による日付ズレバグの解消。
   - JSONパース自動補正（`safeParseJson`）によるフォールバック誤発動防止。

7. **卒業制作プレゼン資料**:
   - `docs/presentation_oboeru_kun.md` に6分間の卒業プレゼンスライド構成＆原稿を完備。

---

## 9. Claude Code（AIエージェント）への引き継ぎメモ

Claude Codeで作業を継続する場合：
- プロジェクト直下の `oboeru-kun-handoff.md`（本書）を読めば、全体の設計思想から最新のコード構造まで完全に把握できます。
- モデル設定ルールは `model_selection_guide.md`（爆速: `claude-haiku-4-5-20251001`、高精度: `claude-sonnet-5`）を参照してください。
- プレゼン構成は `docs/presentation_oboeru_kun.md` にまとまっています。
- 直近の作業内容は §10 を参照してください。

---

## 10. 2026-08-22 のレビュー・修正セッション記録

UI/UXデザイナー・シニアエンジニアの2サブエージェントによるレビューを実施し、
指摘に基づいて以下を順に修正した（コミット順）。詳細な理由・失敗シナリオは
各コミットメッセージに記載しているので、`git log` で辿れる。

### 致命的（機能が完全に死んでいた）
- **聞き返しチャットが毎回API 400 → 定型フォールバックしか返らない**
  （`lib/anthropic.ts`: messages配列の先頭がassistantロールだった。Messages APIは
  先頭がuserでないと400を返す。挨拶はsystemプロンプトに移した）
- **PDF取り込みが100%失敗**（pdf-parse v2はモジュールが関数ではなくPDFParseクラスを
  export する。v1形式の呼び出しをv2 APIに移行）
- **AUTH_PASSCODEがGit履歴に旧サンプル値のまま残っていた**（ユーザー自身が
  ランダム値に変更・Vercel再デプロイ済み）

### 重要（データ整合性・セキュリティ）
- 採点のレベル計算をクライアント申告値ではなくサーバー（DB）側の値を正とするよう変更
- レート制限のキーが`x-forwarded-for`左端（詐称可能）だったのを右端優先に変更
- 採点の`max_tokens`不足（1500→4000）による解説の途中切れ・JSON破損を修正
- 全クエリに`user_id`境界（`.eq('user_id', 'default_user')`）を追加
- FileImporterが409/500を握り潰し「件数もエラーも出ない」問題を修正

### UI/UX（設計思想の破れ）
- 用語0件の初回ユーザーに「本日の復習はすべて完了！🎉」が出ていた問題を修正
  （空状態を専用画面に分離）
- 「わからん」の正直申告にシェイク・雨エフェクトが発火していた問題を修正
  （設計思想②「わからんも責めずに拾う」の直接的な破れだった）
- 採点スコアの閾値（80/50）が3ファイルでバラバラだったのを`lib/constants.ts`の
  `SCORE_PROMOTE`/`SCORE_KEEP`に一元化
- 先取り復習・集中特訓が忘却曲線のスケジュールを上書きする問題を修正
  （前倒し出題時は復習日を早める方向にしか動かさない）
- 出題中に中断する導線がなかった問題を修正（「← 中断する」ボタン＋
  スワイプバック対策）
- 通知チャネル・宛先メールをUIから設定できるようにした
  （`app/api/settings/route.ts`新規、`lib/notificationSettings.ts`新規）

### 用語抽出の精度（このセッションの最終トピック）
既存の抽出プロンプトが「講義中の言い回し・行動フレーズ・章タイトル」を
専門用語として拾ってしまう問題があった（例:「面倒の正体」「完成形の絵から作る」
「進捗可視化ツール」）。判定軸を **「その講義を知らない同僚に見せて意味が通じるか」**
の単独テスト一本に整理し、`lib/anthropic.ts`の`EXTRACT_CRITERIA`として
テキスト・画像両方の抽出プロンプトで共有するようにした。

あわせて、複数レッスンを1ファイルにまとめた講義HTMLで用語を大幅に
取りこぼす別のバグも発覚・修正した。原因は3つ重なっていた:
1. HTMLをCSSごとそのままAIに渡していた（`htmlToText()`を追加して除去）
2. 先頭30000文字で切り捨てていた（→120000文字に引き上げ）
3. 抽出件数を固定で0〜5個に制限していた（→「1レッスンあたり0〜5個を
   目安に、レッスンごとに数え直す」方式に変更）

実データ2件（8〜9レッスン分のHTML、各20〜30件の用語解説を含む）で検証し、
どちらも最終レッスンまで正しくカバーし、ノイズも大部分排除できることを確認済み。
ただし完全ではなく、「ドメイン直下」「強制力」のような一般語・位置説明が
稀に混入することがある（棚卸し機能で拾える範囲）。

**既存の登録済み用語を後から同じ基準で棚卸しする機能**
（`components/TermAuditModal.tsx`、`app/api/terms/audit/route.ts`）も
このセッションで新規追加した。判定するだけで削除はせず、候補は必ずユーザーが
チェックボックスで選んでから削除する。あわせて全削除ボタン
（`components/DeleteAllTermsModal.tsx`、件数入力による二重確認）も追加した。

### 未対応・今後の検討事項
- UI/UXレビューのR-6以降（通知チャネルの時刻設定はVercel Cronのスケジュール
  変更が必要なため未着手）、その他レビューで「改善推奨」「細かい磨き込み」に
  分類された項目は未着手。
- 用語抽出の「Slack」のような境界事例（言及されただけの固有名詞）が
  稀に混入する。実害は小さいので許容しているが、気になれば基準を追加で
  厳格化できる。

---

## 11. 2026-08-23 の新機能追加：特急・4択復習モード（スキマ時間対応）

「自分の言葉で説明する（記述・音声）」コアバリューを維持しつつ、忙しい日や電車移動中など長文入力や音声が出せない場面でも学習継続（離脱防止・ゼロの日撲滅）ができるよう、**AI（Claude Haiku 4.5）即時生成による「特急・4択復習モード」** を追加した。

### 実装内容
1. **4択クイズ生成エンドポイント**（`app/api/quiz/mcq/generate/route.ts`、`lib/anthropic.ts` の `generateMultipleChoiceQuiz`）:
   - Claude Haiku 4.5 (`claude-haiku-4-5-20251001`) を使用。
   - 用語の本質を突いた正解1つと、初心者が混同しやすい実践的なダミー3つをAIが即時生成（約1秒・max_tokens: 800）。
   - コーチの人格（大阪弁等）に合わせた出題文・ツッコミ・各選択肢の1行解説を同梱。
2. **4択回答・忘却曲線更新エンドポイント**（`app/api/quiz/mcq/answer/route.ts`）:
   - 正解時: 85点（合格扱い）、レベル昇格（0→1, 1→2..）、次回復習日を更新。
   - 不正解時: 30点、レベル0にリセット（翌日再出題）。
   - 先取り復習時は間隔を前倒し方向にのみ更新。
3. **4択専用出題UI**（`components/QuickQuizSession.tsx`）:
   - 和モダンデザインに統一された4つの選択肢ボタン。
   - タップ直後に即時判定（⭕️ 正解 / ❌ あなたの選択）とスタンプ・エフェクト（桜吹雪/シェイク）を発火。
   - 各選択肢の下にコーチの一言ミニ解説が展開。
   - 「次のお題へ ➔」でサクサク進行。
4. **ホーム画面の導線統合**（`app/page.tsx`）:
   - 今日の復習カード・分野別特訓カード・ノルマ完了カードのすべてから、「✍️ 記述復習」と「⚡️ 特急4択」をその日の状況に合わせてワンタップで選択可能。

---

## 12. 2026-08-23 の新機能追加：教材タイトルのAI自動分別＆スマートタギング

ファイル名（`web-kougeki2.html`等）から無機質なローマ字タグが作られていた問題を解消し、教材の正式な日本語タイトルをAIが自動抽出し、既存分野と自動分別・名寄せする仕組みを実装した。

### 実装内容
1. **分野名のAI自動推定＆既存タグへのスマート名寄せ**（`lib/anthropic.ts` の `extractTermsFromText`, `extractTermsFromImage`, `app/api/extract/route.ts`）:
   - 教材HTMLの`<title>`や見出し、本文の文脈から「Webアプリの攻撃と防御」「インターネットの仕組み」などの正式な日本語分野名をAIが自動判定。
   - 既存のタグ一覧をAIに渡し、「今回のファイルが既存分野の続きか」を判定して既存タグに自動でまとめる（スマート名寄せ）。
2. **インポート確認画面でのタグ設定UI**（`components/FileImporter.tsx`）:
   - AIが推定した分野名を上部に表示し、ユーザーがその場で確認・編集可能。
   - 既存タグからのワンタップ選択チップも配置。
3. **既存タグの一括リネーム機能**（`app/api/terms/rename-tag/route.ts`, `components/RenameTagModal.tsx`, `app/page.tsx`）:
   - 集中特訓カードのタイトル横に「✏️ 分野名を変更」ボタンを新設。
   - 登録済みの `web kougeki2` などを一発で「Webアプリの攻撃と防御」に一括変更可能。


